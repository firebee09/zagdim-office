{
  "name": "zagdim-2d-office",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "server": "cd server && npm install && npm run dev",
    "client": "cd client && npm install && npm start",
    "install:all": "cd server && npm install && cd ../client && npm install"
  }
}
